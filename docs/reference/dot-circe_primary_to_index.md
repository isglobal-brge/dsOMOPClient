# Convert one Circe PrimaryCriteria item to an explicit index event

Convert one Circe PrimaryCriteria item to an explicit index event

## Usage

``` r
.circe_primary_to_index(
  primary,
  concept_sets,
  primary_limit,
  end_strategy = NULL
)
```
