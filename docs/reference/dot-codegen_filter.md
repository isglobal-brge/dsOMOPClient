# Generate R code string for a single omop_filter

Generate R code string for a single omop_filter

## Usage

``` r
.codegen_filter(f)
```

## Arguments

- f:

  An `omop_filter` object.

## Value

Character; R code that recreates the filter.
