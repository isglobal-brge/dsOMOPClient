# Collect all population-level filters from a recipe, flattening groups

Collect all population-level filters from a recipe, flattening groups

## Usage

``` r
.collect_pop_filters(recipe)
```

## Arguments

- recipe:

  An `omop_recipe` object.

## Value

List of `omop_filter` objects at the `"population"` level.
