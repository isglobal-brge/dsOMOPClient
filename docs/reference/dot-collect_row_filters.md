# Collect all row-level filters from a recipe, flattening groups

Collect all row-level filters from a recipe, flattening groups

## Usage

``` r
.collect_row_filters(recipe)
```

## Arguments

- recipe:

  An `omop_recipe` object.

## Value

List of `omop_filter` objects at the `"row"` level.
