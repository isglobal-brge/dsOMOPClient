# Pool counts by summing across servers

Pool counts by summing across servers

## Usage

``` r
.pool_counts(per_site_counts, policy = "strict")
```

## Arguments

- per_site_counts:

  Named numeric vector or list of counts

- policy:

  Character; "strict" or "pooled_only_ok"

## Value

List with \$result and \$warnings
