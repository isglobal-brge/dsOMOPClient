# Executable Recipe filter contract

Kept in one place so constructors, imported recipes, linting and tests
use the same type-by-level matrix that the server accepts.

## Usage

``` r
.recipeFilterContract()
```

## Value

Named list mapping filter levels to executable filter types.
