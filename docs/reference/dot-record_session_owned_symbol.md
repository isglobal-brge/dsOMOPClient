# Record an owned server symbol without changing the default manipulation input

Record an owned server symbol without changing the default manipulation
input

## Usage

``` r
.record_session_owned_symbol(session_symbol, symbol)
```

## Arguments

- session_symbol:

  Local OMOP session name.

- symbol:

  Newly committed server-side symbol.

## Value

\`NULL\`, invisibly.
