# Print an omop_filter

Print an omop_filter

## Usage

``` r
# S3 method for class 'omop_filter'
print(x, ...)
```

## Arguments

- x:

  An `omop_filter` object.

- ...:

  Additional arguments (ignored).

## Value

`x`, invisibly.
