# Print an omop_filter_group

Print an omop_filter_group

## Usage

``` r
# S3 method for class 'omop_filter_group'
print(x, ...)
```

## Arguments

- x:

  An `omop_filter_group` object.

- ...:

  Additional arguments (ignored).

## Value

`x`, invisibly.
