# Remove a variable from the recipe

Removes a variable by name from the recipe's variable list.

## Usage

``` r
recipe_remove_variable(recipe, name)
```

## Arguments

- recipe:

  An `omop_recipe` object.

- name:

  Character; variable name to remove.

## Value

The modified `omop_recipe` object.

## See also

[`omop_recipe`](https://isglobal-brge.github.io/dsOMOPClient/reference/omop_recipe.md)
