% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ohdsi_results.R
\name{ds.omop.ohdsi.results}
\alias{ds.omop.ohdsi.results}
\title{Query an OHDSI result table}
\usage{
ds.omop.ohdsi.results(
  table_name,
  columns = NULL,
  filters = NULL,
  order_by = NULL,
  limit = 5000L,
  tool_id = NULL,
  scope = c("per_site", "pooled"),
  pooling_policy = "strict",
  symbol = "omop",
  conns = NULL,
  type = NULL
)
}
\arguments{
\item{table_name}{Character; which result table to query.}

\item{columns}{Character vector; columns to select (NULL = all safe columns).}

\item{filters}{Named list; WHERE conditions.}

\item{order_by}{Character; ORDER BY column.}

\item{limit}{Integer; max rows (capped at 5000 server-side).}

\item{tool_id}{Character; optional tool identifier.}

\item{scope}{Character; \code{"per_site"} or \code{"pooled"}.}

\item{pooling_policy}{Character; \code{"strict"} (default) or
\code{"pooled_only_ok"}.}

\item{symbol}{Character; the session symbol (default: \code{"omop"}).}

\item{conns}{DSI connection object(s). If \code{NULL}, uses active session.}

\item{type}{Optional result view: \code{"split"}, \code{"combine"}, or
\code{"both"}. When omitted, legacy \code{scope} behaviour is preserved.}
}
\value{
A \code{dsomop_result} object.
}
\description{
Reads rows from a pre-computed OHDSI result table with server-controlled
disclosure. Split-only requests preserve that compatibility endpoint.
Combined requests obtain the physical result table's closed, server-owned
pooling contract and combine complete disclosure-controlled frames through
that contract; they never route through a same-named live analysis or a
column-name pooling heuristic. Reviewed equality filters are applied
server-side; selection, ordering, and the row limit are applied only after
contractual pooling.
}
\examples{
\dontrun{
# Cohort diagnostics results per site
cd <- ds.omop.ohdsi.results("index_event_breakdown")

# Cohort counts pooled across servers
cc <- ds.omop.ohdsi.results("cohort_count", scope = "pooled")
}
}
