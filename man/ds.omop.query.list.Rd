% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/queries.R
\name{ds.omop.query.list}
\alias{ds.omop.query.list}
\title{List available query templates (DEPRECATED)}
\usage{
ds.omop.query.list(
  domain = NULL,
  provider = "native",
  symbol = "omop",
  conns = NULL
)
}
\arguments{
\item{domain}{Character; optional domain filter. \code{NULL} (the default)
returns all domains.}

\item{provider}{Character; ignored (retained for back-compatibility).}

\item{symbol}{Character; the session symbol used when the OMOP connection
was initialised (default: \code{"omop"}).}

\item{conns}{DSI connection object(s). If \code{NULL} (the default), the
connections stored in the active session are used.}
}
\value{
Data frame of analysis-catalog entry metadata (the pooled view).
}
\description{
Deprecated shim for \code{\link{ds.omop.analysis.list}}. The curated query
templates are now part of the unified analysis catalog; this forwards to
\code{ds.omop.analysis.list()} and returns the catalog data frame.
}
\examples{
\dontrun{
templates <- ds.omop.query.list()
head(templates)
}
}
\seealso{
\code{\link{ds.omop.analysis.list}}
}
