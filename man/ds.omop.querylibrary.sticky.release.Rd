% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/querylibrary_sticky.R
\name{ds.omop.querylibrary.sticky.release}
\alias{ds.omop.querylibrary.sticky.release}
\title{Release a pinned QueryLibrary sticky redesign}
\usage{
ds.omop.querylibrary.sticky.release(
  x,
  redesign,
  datasources = NULL,
  pool = TRUE,
  format = c("long", "wide", "vector", "raw")
)
}
\arguments{
\item{x}{Bare DataSHIELD symbol naming the prepared person-local table.}

\item{redesign}{An object created by
\code{\link{omop_querylibrary_sticky}}.}

\item{datasources}{Named DataSHIELD connection list. \code{NULL} uses active
connections.}

\item{pool}{Pool compatible releases across sites. For
\code{bounded_distinct}, pooling is the sum of site-local cardinalities,
not a cross-site set union; use \code{FALSE} for site-specific estimates.}

\item{format}{Output format passed to \code{\link{ds.omop.dp.release}}.}
}
\value{
A \code{dsomop_result} from \code{\link{ds.omop.dp.release}}.
}
\description{
Verifies that every selected server advertises the same pinned semantic
redesign, then delegates to \code{\link{ds.omop.dp.release}}. \code{x} must be
the single memory-mode output assigned by the Recipe/Plan preparation. The
upstream QueryLibrary SQL is never submitted to a server. No client argument
can control epsilon, seed, nonce, epoch, or rerolls.
}
