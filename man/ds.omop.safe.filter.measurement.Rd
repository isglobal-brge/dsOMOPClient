% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exploration.R
\name{ds.omop.safe.filter.measurement}
\alias{ds.omop.safe.filter.measurement}
\title{Create a safe population filter for a numeric measurement interval}
\usage{
ds.omop.safe.filter.measurement(
  concept_id,
  min_value,
  max_value,
  n_bins = 10L,
  symbol = "omop",
  conns = NULL
)
}
\arguments{
\item{concept_id}{One measurement concept ID.}

\item{min_value, max_value}{Finite requested interval limits. Both are
required and must lie inside the common public grid.}

\item{n_bins}{Integer; the number of bins for cutpoint computation
(default: 10).}

\item{symbol}{Character; the session symbol (default: \code{"omop"}).}

\item{conns}{DSI connection object(s) or NULL to use the session default.}
}
\value{
An authenticated population-level \code{omop_filter}.
}
\description{
Requests the controller-configured public grid for one measurement concept,
then snaps the requested closed-open interval outwards to edges issued by
every connected site. The result is executable as a population-level
\code{has_measurement} filter. Exact or one-sided client-authored thresholds
are deliberately not supported.
}
